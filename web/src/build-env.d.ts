// Compile-time constants injected by Vite `define` (see vite.config.ts).
// Replaced with string literals during `vite build`; not present at runtime
// outside a Vite build (so don't reference them from unit-tested modules).
declare const __APP_VERSION__: string;
declare const __BUILD_SHA__: string | null;
