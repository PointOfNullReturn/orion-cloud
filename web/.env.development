# Loaded by Vite in dev (`npm run dev`). Non-secret — safe to commit.
# Points at the .NET API running via `dotnet run` (http profile), which uses
# ASPNETCORE_ENVIRONMENT=Development — the only local mode whose CORS policy
# allows the Vite origin (http://localhost:5173).
VITE_API_URL=http://localhost:5204
